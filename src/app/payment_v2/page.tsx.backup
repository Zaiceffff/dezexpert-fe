// src/app/payment_v2/page.tsx — страница тарифов Dezexpert
'use client';
import { useState, useEffect } from 'react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
import Footer from '@/components/Footer';
import { Check, Star, Zap, Shield, Users, BarChart3, MessageSquare, Globe, Settings, Crown, RefreshCw } from 'lucide-react';
import { toast } from 'sonner';
import { apiClient } from '@/lib/api';

export default function PaymentPage() {
  const [loadingStates, setLoadingStates] = useState<{[key: string]: boolean}>({});
  const [currentUser, setCurrentUser] = useState<any>(null);
  const [userTariff, setUserTariff] = useState<any>(null);
  const [plans, setPlans] = useState<any[]>([]);
  const [isLoadingPlans, setIsLoadingPlans] = useState(true);
  const [expandedFeatures, setExpandedFeatures] = useState<{[key: string]: boolean}>({});
  const [isSwitchingToTrial, setIsSwitchingToTrial] = useState(false);

  const [enterprise, setEnterprise] = useState({
    name: 'Корпоративный',
    description: 'Индивидуальные решения для крупных компаний',
    features: [
      'Неограниченное количество пользователей',
      'Индивидуальная разработка функций',
      'Выделенный сервер',
      'SLA гарантии',
      'Обучение персонала',
      'Интеграция с корпоративными системами',
      'Персональный аккаунт-менеджер'
    ]
  });

  // Функция для получения токена авторизации
  const getAuthToken = (): string | null => {
    return localStorage.getItem('auth_token') || 
           localStorage.getItem('token') || 
           sessionStorage.getItem('token');
  };

  // Функция для загрузки тарифов из API
  const fetchTariffs = async () => {
    try {
      const response = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/tariff/list`, {
        headers: {
          'accept': 'application/json'
        }
      });

      if (response.ok) {
        const tariffsData = await response.json();
        
        // Фильтруем нужные тарифы: "Пробный план", "Всегда вместе", "Всегда вместе +", "Всегда вместе Промо", "Корпоративный"
        const allowedTariffNames = [
          'Пробный план',
          'План\n"Всегда вместе"',
          'План\n"Всегда вместе+"',
          'План\r\n"Всегда вместе Промо"',
          'План "Корпоративный"'
        ];
        
        const activeTariffs = tariffsData
          .filter((tariff: any) => 
            tariff.isActive && 
            allowedTariffNames.some(name => tariff.name.includes(name.replace(/План\s*["\n\r]/g, '').trim()))
          )
          .map((tariff: any) => ({
            id: tariff.id,
            name: tariff.name.replace(/План\s*["\n\r]/g, '').trim(), // Убираем "План" и переносы строк
            price: tariff.price === '0' ? '0 ₽' : 
                   tariff.name.includes('Корпоративный') ? 'ОБСУДИМ' : `${tariff.price} ₽`,
            period: tariff.price === '0' ? 'навсегда' : 
                    tariff.name.includes('Корпоративный') ? '' : 
                    tariff.name.includes('Промо') ? ' (акция)' : '/месяц',
            description: getTariffDescription(tariff.name, tariff.price),
            popular: tariff.name.includes('Всегда вместе') && !tariff.name.includes('+') && !tariff.name.includes('Промо'), // "Всегда вместе" как популярный (но не промо)
            features: tariff.advantages,
            cta: getTariffCTA(tariff.name, tariff.price),
            href: tariff.price === '0' ? '/register' : null,
            isFree: tariff.price === '0' && !tariff.name.includes('Корпоративный') && !tariff.name.includes('Промо'),
            isPromo: tariff.isPromo,
            originalPrice: tariff.price
          }));

        // Сортируем тарифы: бесплатный первый, затем по цене, корпоративный последний
        const sortedTariffs = activeTariffs.sort((a: any, b: any) => {
          if (a.isFree) return -1;
          if (b.isFree) return 1;
          if (a.name.includes('Корпоративный')) return 1;
          if (b.name.includes('Корпоративный')) return -1;
          return parseInt(a.originalPrice) - parseInt(b.originalPrice);
        });

        setPlans(sortedTariffs);

        // Корпоративный тариф теперь включен в основную сетку
        // Обновляем enterprise для отдельной секции
        const corporateTariff = tariffsData.find((tariff: any) => 
          tariff.isActive && tariff.name.includes('Корпоративный')
        );

        if (corporateTariff) {
          setEnterprise({
            name: corporateTariff.name.replace(/План\s*["\n\r]/g, '').trim(),
            description: 'Индивидуальные решения для крупных компаний',
            features: corporateTariff.advantages
          });
        }
      } else {
        throw new Error(`Ошибка ${response.status}: ${response.statusText}`);
      }
    } catch (error) {
      console.error('Ошибка при загрузке тарифов:', error);
      toast.error('Не удалось загрузить тарифы');
    } finally {
      setIsLoadingPlans(false);
    }
  };

  // Функция для генерации описания тарифа
  const getTariffDescription = (name: string, price: string): string => {
    if (price === '0') return 'Идеально для тестирования и небольших компаний';
    if (name.includes('Всегда вместе') && !name.includes('+') && !name.includes('Промо')) return 'Для растущих компаний с активным потоком заявок';
    if (name.includes('Всегда вместе+')) return 'Для команд и компаний с высокими требованиями';
    if (name.includes('Всегда вместе Промо')) return 'Специальное предложение для быстрого старта';
    if (name.includes('Корпоративный')) return 'Индивидуальные решения для крупных компаний';
    return 'Оптимальное решение для вашего бизнеса';
  };

  // Функция для генерации текста кнопки
  const getTariffCTA = (name: string, price: string): string => {
    if (price === '0') return 'Начать бесплатно';
    if (name.includes('Корпоративный')) return 'Связаться';
    if (name.includes('Всегда вместе')) return 'Выбрать план';
    return 'Выбрать план';
  };

  // Функция для переключения развернутого состояния функций
  const toggleFeaturesExpansion = (planId: string) => {
    setExpandedFeatures(prev => ({
      ...prev,
      [planId]: !prev[planId]
    }));
  };

  // Функция для получения информации о пользователе
  const fetchUserInfo = async () => {
    const token = getAuthToken();
    if (!token) return;

    try {
      const response = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/profile`, {
        headers: {
          'Authorization': `Bearer ${token}`,
          'Content-Type': 'application/json'
        }
      });

      if (response.ok) {
        const userData = await response.json();
        console.log('📊 Данные пользователя:', userData);
        console.log('🔍 Статус пользователя:', userData.status);
        console.log('🔍 Объект tariff:', userData.tariff);
        console.log('🔍 Цена тарифа:', userData.tariff?.price);
        setCurrentUser(userData);

        // Логика определения текущего тарифа пользователя
        let tariffInfo = null;
        
        // Приоритет: сначала проверяем объект tariff (оплаченный тариф)
        if (userData.tariff && userData.tariff.id && userData.tariff.price !== '0') {
          // Если есть объект tariff с ID и ценой не 0 - это оплаченный тариф
          tariffInfo = userData.tariff;
          console.log('Обнаружен оплаченный тариф:', tariffInfo);
        } else if (userData.status === 'Trial' && (!userData.tariff || userData.tariff.price === '0')) {
          // Если статус Trial И нет тарифа или тариф бесплатный - это пробный план
          tariffInfo = { id: 'trial', name: 'Пробный план', price: '0 ₽', period: 'навсегда' };
          console.log('Статус Trial + нет оплаченного тарифа — пробный план:', tariffInfo);
        } else if (userData.status === 'Active' && userData.tariff) {
          // Если статус Active и есть объект tariff - это оплаченный тариф
          tariffInfo = userData.tariff;
          console.log('Статус Active + объект tariff - оплаченный тариф:', tariffInfo);
        } else if (userData.tariff) {
          // Если есть объект tariff, но статус не Active (возможно переходное состояние)
          tariffInfo = userData.tariff;
          console.log('Есть объект tariff, но статус не Active (переходное состояние):', tariffInfo);
        }
        
        // Если тариф определен, устанавливаем его
        if (tariffInfo) {
          setUserTariff(tariffInfo);
          return;
        }
        
        // Пытаемся получить информацию о тарифе из разных возможных полей
        if (userData.tariff) {
          tariffInfo = userData.tariff;
        } else if (userData.tariffPlan) {
          tariffInfo = userData.tariffPlan;
        } else if (userData.subscription) {
          tariffInfo = userData.subscription;
        } else if (userData.currentTariff) {
          tariffInfo = userData.currentTariff;
        }
        
        console.log('Информация о тарифе из профиля:', tariffInfo);
        
        // Если не удалось получить информацию о тарифе из профиля, 
        // попробуем получить из отдельного API
        if (!tariffInfo) {
          try {
            const tariffResponse = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/service`, {
              headers: {
                'Authorization': `Bearer ${token}`,
                'Content-Type': 'application/json'
              }
            });
            
            if (tariffResponse.ok) {
              const serviceData = await tariffResponse.json();
              console.log('Данные о сервисе/тарифе:', serviceData);
              
              // Пытаемся извлечь информацию о тарифе
              if (serviceData.tariff) {
                tariffInfo = serviceData.tariff;
              } else if (serviceData.currentPlan) {
                tariffInfo = serviceData.currentPlan;
              }
            }
          } catch (tariffError) {
            console.log('Не удалось получить информацию о тарифе из /user/service:', tariffError);
          }
        }
        
        console.log('Финальная информация о тарифе:', tariffInfo);
        setUserTariff(tariffInfo);
      }
    } catch (error) {
      console.error('Ошибка при получении информации о пользователе:', error);
    }
  };

  // Функция для создания платежной ссылки
  const createPaymentLink = async (tariffId: string) => {
    const token = getAuthToken();
    if (!token) {
      toast.error('Требуется авторизация');
      return;
    }

    setLoadingStates(prev => ({ ...prev, [tariffId]: true }));
    try {
      const response = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/payment/payment-link/${tariffId}`, {
        headers: {
          'Authorization': `Bearer ${token}`,
          'Content-Type': 'application/json'
        }
      });

      if (response.ok) {
        const responseData = await response.json();
        
        // Проверяем структуру ответа и извлекаем ссылку
        let paymentLink;
        if (typeof responseData === 'string') {
          // Если API возвращает прямую строку
          paymentLink = responseData;
        } else if (responseData.paymentLink) {
          // Если API возвращает объект с paymentLink
          paymentLink = responseData.paymentLink;
        } else if (responseData.url) {
          // Если API возвращает объект с url
          paymentLink = responseData.url;
        } else if (responseData.link) {
          // Если API возвращает объект с link
          paymentLink = responseData.link;
        } else {
          console.error('Неожиданный формат ответа API:', responseData);
          throw new Error('Неверный формат ответа от API');
        }
        
        // Проверяем, что ссылка валидная и ведет на правильный домен
        if (!paymentLink || !paymentLink.startsWith('http')) {
          throw new Error('Получена неверная ссылка от API');
        }
        
        // Проверяем, что ссылка ведет на merch.tochka.com
        if (!paymentLink.includes('merch.tochka.com')) {
          console.warn('Ссылка ведет не на ожидаемый домен:', paymentLink);
        }
        
        // Автоматический редирект с dezexpert.pro на текущий домен для разработки
        if (paymentLink.includes('dezexpert.pro')) {
          const currentOrigin = window.location.origin;
          const localPaymentLink = paymentLink.replace('https://dezexpert.pro', currentOrigin);
          console.log('Перенаправляем на текущий домен:', localPaymentLink);
          paymentLink = localPaymentLink;
        }
        
        console.log('Создана платежная ссылка:', paymentLink);
        
        // Открываем платежную ссылку в новом окне
        window.open(paymentLink, '_blank');
        toast.success('Платежная ссылка создана!');
      } else {
        throw new Error(`Ошибка ${response.status}: ${response.statusText}`);
      }
    } catch (error) {
      console.error('Ошибка при создании платежной ссылки:', error);
      
      // Показываем более информативное сообщение об ошибке
      if (error instanceof Error) {
        toast.error(`Ошибка: ${error.message}`);
      } else {
        toast.error('Не удалось создать платежную ссылку');
      }
    } finally {
      setLoadingStates(prev => ({ ...prev, [tariffId]: false }));
    }
  };

  // Функция для обработки выбора тарифа
  const handleTariffSelect = async (plan: any) => {
    if (plan.isFree) {
      // Для бесплатного тарифа - переход на регистрацию
      window.location.href = plan.href;
      return;
    }

    // Для корпоративного тарифа - переход на контактную форму
    if (plan.name.includes('Корпоративный')) {
      window.location.href = '/app/dashboard';
      return;
    }

    // Для платных тарифов проверяем авторизацию
    const token = getAuthToken();
    if (!token) {
      toast.error('Для выбора платного тарифа требуется авторизация');
      return;
    }

    // Создаем платежную ссылку
    await createPaymentLink(plan.id);
  };

  // Загружаем информацию о пользователе и тарифы при монтировании компонента
  useEffect(() => {
    fetchTariffs();
    fetchUserInfo();
  }, []);

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-green-50 to-emerald-50 flex flex-col">
      <div className="flex-1">
        {/* Header Section */}
        <section className="pt-16 sm:pt-20 pb-12 sm:pb-16 px-4 sm:px-6 lg:px-8">
          <div className="max-w-4xl mx-auto text-center">
            <h1 className="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-4 sm:mb-6">
              Выберите подходящий тариф
            </h1>
            <p className="text-lg sm:text-xl text-gray-600 leading-relaxed">
              Прозрачное ценообразование без скрытых комиссий. Начните бесплатно и масштабируйтесь по мере роста.
            </p>
            
            {/* Информация о текущем тарифе */}
            {currentUser && userTariff ? (
              <div className="mt-8 relative">
                {/* Декоративный фон */}
                <div className="absolute inset-0 bg-gradient-to-r from-green-100/50 via-emerald-100/30 to-teal-100/50 rounded-3xl blur-xl"></div>
                
                <div className="relative bg-white/90 backdrop-blur-sm rounded-3xl border border-green-200/50 shadow-xl overflow-hidden">
                  {/* Верхняя полоса с градиентом */}
                  <div className="h-2 bg-gradient-to-r from-green-500 via-emerald-500 to-teal-500"></div>
                  
                  <div className="p-8">
                    <div className="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6">
                      {/* Основная информация о тарифе */}
                      <div className="flex-1">
                        <div className="flex items-center gap-3 mb-4">
                          <div className="w-12 h-12 bg-gradient-to-r from-green-500 to-emerald-500 rounded-xl flex items-center justify-center shadow-lg">
                            <Crown className="w-6 h-6 text-white" />
                          </div>
                          <div>
                            <p className="text-sm font-medium text-green-600 uppercase tracking-wide">Текущий тариф</p>
                            <h3 className="text-2xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">
                              {typeof userTariff === 'string' ? userTariff : (userTariff.name || userTariff.planName || 'Не выбран')}
                            </h3>
                          </div>
                        </div>
                        
                        {/* Детали тарифа */}
                        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                          {userTariff && typeof userTariff === 'object' && userTariff.price && (
                            <div className="bg-gradient-to-r from-green-50 to-emerald-50 rounded-xl p-4 border border-green-100">
                              <p className="text-xs font-medium text-green-600 uppercase tracking-wide mb-1">Стоимость</p>
                              <p className="text-2xl font-bold text-green-700">
                                {userTariff.price} <span className="text-sm font-normal text-green-600">{userTariff.period || '/месяц'}</span>
                              </p>
                            </div>
                          )}
                          
                          <div className="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl p-4 border border-blue-100">
                            <p className="text-xs font-medium text-blue-600 uppercase tracking-wide mb-1">Статус</p>
                            <div className="flex items-center gap-2">
                              <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
                              <p className="text-lg font-semibold text-blue-700">
                                {currentUser?.status || 'Не определен'}
                              </p>
                            </div>
                          </div>
                        </div>
                        
                        {/* ID тарифа */}
                        <div className="mt-4 bg-gray-50 rounded-lg p-3 border border-gray-200">
                          <p className="text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">ID тарифа</p>
                          <p className="text-sm font-mono text-gray-700 bg-white px-3 py-1 rounded border">
                            {userTariff.id}
                          </p>
                        </div>
                      </div>
                      
                      {/* Кнопка действия */}
                      {userTariff && typeof userTariff === 'object' && userTariff.id !== 'trial' && userTariff.id !== 'basic' && userTariff.price !== '0' && (
                        <div className="flex flex-col gap-2">
                          <Button
                            onClick={async () => {
                          const token = getAuthToken();
                          if (!token) {
                            toast.error('Требуется авторизация');
                            return;
                          }

                                                  setIsSwitchingToTrial(true);
                        try {
                          console.log('🔄 Переводим пользователя на пробный период (с бэкендом)...');
                          console.log('📊 Текущий тариф:', userTariff);

                          // 1) Попробуем отменить подписку (если есть)
                          try {
                            await apiClient.cancelSubscription();
                          } catch (e) {
                            console.warn('⚠️ Не удалось вызвать cancelSubscription, продолжаем:', e);
                          }

                          // 2) Выставим статус Trial на бэкенде и отвяжем тариф
                          const userId = currentUser?.id;
                          if (userId) {
                            try {
                              await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/by-id/${userId}`, {
                                method: 'PATCH',
                                headers: {
                                  'Authorization': `Bearer ${token}`,
                                  'Content-Type': 'application/json'
                                },
                                body: JSON.stringify({ status: 'Trial' })
                              });
                            } catch (e) {
                              console.warn('⚠️ Не удалось обновить статус пользователя на Trial:', e);
                            }

                            let tariffCleared = false;
                            try {
                              const resp = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/by-id/${userId}`, {
                                method: 'PATCH',
                                headers: {
                                  'Authorization': `Bearer ${token}`,
                                  'Content-Type': 'application/json'
                                },
                                body: JSON.stringify({ tariffId: null })
                              });
                              tariffCleared = resp.ok;
                            } catch {}

                            if (!tariffCleared) {
                              try {
                                await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/by-id/${userId}`, {
                                  method: 'PATCH',
                                  headers: {
                                    'Authorization': `Bearer ${token}`,
                                    'Content-Type': 'application/json'
                                  },
                                  body: JSON.stringify({ tariff: null })
                                });
                              } catch (e) {
                                console.warn('⚠️ Не удалось явно отвязать тариф:', e);
                              }
                            }
                          }

                          // 3) Сначала обновляем информацию о пользователе, чтобы получить актуальные данные из базы
                          await fetchUserInfo();
                          
                          // 4) Проверяем, что статус действительно изменился на Trial
                          const updatedResponse = await fetch(`${process.env.HOST || 'https://api.bugbot.ru'}/user/profile`, {
                            headers: {
                              'Authorization': `Bearer ${token}`,
                              'Content-Type': 'application/json'
                            }
                          });
                          
                          if (updatedResponse.ok) {
                            const updatedUserData = await updatedResponse.json();
                            console.log('📊 Обновленные данные пользователя:', updatedUserData);
                            
                            if (updatedUserData.status === 'Trial') {
                              toast.success('Переведено на пробный период!');
                            } else {
                              toast.warning('Статус обновлен, но может потребоваться время для полного применения изменений');
                            }
                          } else {
                            toast.success('Запрос на переход на пробный период отправлен!');
                          }
                        } catch (error: unknown) {
                          console.error('❌ Ошибка при переходе на пробный период:', error);
                          toast.error('Не удалось перевести на пробный период');
                        } finally {
                          setIsSwitchingToTrial(false);
                        }
                        }}
                        className="bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white px-8 py-4 rounded-2xl text-lg font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
                        disabled={isSwitchingToTrial}
                      >
                        {isSwitchingToTrial ? (
                          <span className="flex items-center gap-3">
                            <span className="h-5 w-5 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                            Переход...
                          </span>
                        ) : (
                          <span className="flex items-center gap-2">
                            <RefreshCw className="w-5 h-5" />
                            Перейти на пробный
                          </span>
                        )}
                      </Button>
                          <p className="text-xs text-gray-500 text-center max-w-xs">
                            Отмена текущей подписки и переход на бесплатный пробный период
                          </p>
                        </div>
                    </div>
                  )}
                </div>
              </div>
            ) : (
              <div className="mt-8 p-4 bg-blue-50 rounded-lg border border-blue-200">
                <div className="flex items-center justify-center gap-4">
                  <div className="text-center">
                    <p className="text-sm text-blue-600 mb-2">Для выбора платных тарифов требуется авторизация</p>
                    <div className="flex gap-3">
                      <Button asChild size="sm" className="bg-blue-600 hover:bg-blue-700">
                        <Link href="/login">Войти</Link>
                      </Button>
                      <Button asChild variant="outline" size="sm" className="border-blue-300 text-blue-600 hover:bg-blue-50">
                        <Link href="/register">Регистрация</Link>
                      </Button>
                    </div>
                  </div>
                </div>
              </div>
            )}
          </div>
        </section>

      {/* Pricing Plans */}
      <section className="px-4 sm:px-6 lg:px-8 pb-16 relative">
        {/* Декоративный фон */}
        <div className="absolute inset-0 bg-gradient-to-br from-green-50/30 via-transparent to-emerald-50/30 rounded-3xl"></div>
        <div className="relative max-w-7xl mx-auto">
          {isLoadingPlans ? (
            <div className="flex justify-center items-center py-20">
              <div className="text-center">
                <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-green-600 mx-auto mb-4"></div>
                <p className="text-gray-600">Загрузка тарифов...</p>
              </div>
            </div>
          ) : (
            <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
            {plans.map((plan, index) => (
              <div
                key={plan.name}
                className={`relative rounded-2xl sm:rounded-3xl border-0 bg-gradient-to-br from-white to-gray-50/50 p-6 sm:p-8 transition-all duration-500 hover:scale-105 hover:shadow-2xl flex flex-col min-h-[500px] shadow-lg ${
                  plan.popular 
                    ? 'ring-4 ring-green-500/20 shadow-green-200/50 bg-gradient-to-br from-green-50/30 to-white' 
                    : 'hover:ring-2 hover:ring-green-300/30 hover:shadow-green-100/30'
                }`}
              >
                {plan.popular && (
                  <div className="absolute -top-4 sm:-top-5 left-1/2 transform -translate-x-1/2">
                    <div className="rounded-full bg-gradient-to-r from-green-500 via-emerald-500 to-teal-500 px-4 sm:px-5 py-2 sm:py-2.5 text-xs sm:text-sm font-semibold text-white flex items-center gap-2 shadow-lg animate-pulse">
                      <Star className="h-3 w-3 sm:h-4 sm:w-4 fill-current" />
                      Популярный выбор
                    </div>
                  </div>
                )}
                {plan.isPromo && (
                  <div className="absolute -top-4 sm:-top-5 right-4">
                    <div className="rounded-full bg-gradient-to-r from-orange-500 via-red-500 to-pink-500 px-4 sm:px-5 py-2 sm:py-2.5 text-xs sm:text-sm font-semibold text-white flex items-center gap-2 shadow-lg animate-bounce">
                      <Zap className="h-3 w-3 sm:h-4 sm:w-4 fill-current" />
                      Промо
                    </div>
                  </div>
                )}

                <div className="text-center mb-6 sm:mb-8 flex-1 flex flex-col justify-center">
                  <h3 className="text-xl sm:text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent mb-3">{plan.name}</h3>
                  <p className="text-sm sm:text-base text-gray-600 mb-4 sm:mb-6 leading-relaxed">{plan.description}</p>
                  <div className="mb-4 p-4 bg-gradient-to-r from-gray-50 to-green-50/30 rounded-xl border border-gray-100">
                    <span className="text-3xl sm:text-4xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">{plan.price}</span>
                    <span className="text-gray-500 text-sm ml-2">{plan.period}</span>
                  </div>
                </div>

                <div className="flex-1 flex flex-col justify-center mb-6">
                  {/* Показываем функции по умолчанию (меньше для корпоративного) */}
                  <ul className="space-y-3 sm:space-y-4">
                    {plan.features.slice(0, plan.name.includes('Корпоративный') ? 3 : 3).map((feature: string, idx: number) => (
                      <li key={idx} className="flex items-start gap-3 p-2 rounded-lg hover:bg-green-50/50 transition-all duration-300 hover:scale-105">
                        <div className="flex-shrink-0 w-5 h-5 bg-green-100 rounded-full flex items-center justify-center transition-all duration-300 hover:bg-green-200 hover:scale-110">
                          <Check className="h-3 w-3 text-green-600" />
                        </div>
                        <span className="text-sm text-gray-700 leading-relaxed">{feature}</span>
                    </li>
                  ))}
                </ul>
                  
                  {/* Если функций больше показанных по умолчанию, показываем кнопку "Показать еще" */}
                  {plan.features.length > (plan.name.includes('Корпоративный') ? 3 : 3) && (
                    <div className="mt-4 text-center">
                      <button
                        onClick={() => toggleFeaturesExpansion(plan.id)}
                        onMouseEnter={() => setExpandedFeatures(prev => ({ ...prev, [plan.id]: true }))}
                        onMouseLeave={() => setExpandedFeatures(prev => ({ ...prev, [plan.id]: false }))}
                        className="text-sm text-green-600 hover:text-green-700 font-medium transition-colors duration-200 flex items-center gap-2 mx-auto"
                      >
                        {expandedFeatures[plan.id] ? 'Свернуть' : `Показать еще ${plan.features.length - (plan.name.includes('Корпоративный') ? 3 : 3)}`}
                        <svg 
                          className={`w-4 h-4 transition-transform duration-500 ease-in-out ${expandedFeatures[plan.id] ? 'rotate-180' : ''}`}
                          fill="none" 
                          stroke="currentColor" 
                          viewBox="0 0 24 24"
                        >
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
                        </svg>
                      </button>
                    </div>
                  )}
                  
                  {/* Развернутый список функций (показывается при наведении) */}
                  {expandedFeatures[plan.id] && plan.features.length > (plan.name.includes('Корпоративный') ? 3 : 3) && (
                    <ul className="space-y-3 sm:space-y-4 mt-4 transition-all duration-700 ease-in-out transform opacity-100 scale-100 animate-in slide-in-from-top-2 duration-700">
                      {plan.features.slice(plan.name.includes('Корпоративный') ? 3 : 3).map((feature: string, idx: number) => (
                        <li 
                          key={idx + (plan.name.includes('Корпоративный') ? 3 : 3)} 
                          className="flex items-start gap-3 p-2 rounded-lg hover:bg-green-50/50 transition-all duration-500 ease-in-out hover:scale-105 animate-in slide-in-from-top-2 duration-500"
                          style={{ animationDelay: `${idx * 100}ms` }}
                        >
                          <div className="flex-shrink-0 w-5 h-5 bg-green-100 rounded-full flex items-center justify-center transition-all duration-300 hover:bg-green-200 hover:scale-110">
                            <Check className="h-3 w-3 text-green-600" />
                          </div>
                          <span className="text-sm text-gray-700 leading-relaxed">{feature}</span>
                        </li>
                      ))}
                    </ul>
                  )}
                </div>

                <Button 
                  onClick={() => handleTariffSelect(plan)}
                  disabled={loadingStates[plan.id]}
                  className={`w-full py-3 sm:py-4 text-base sm:text-lg font-semibold mt-auto rounded-xl transition-all duration-300 transform hover:scale-105 ${
                    plan.popular
                      ? 'bg-gradient-to-r from-green-500 via-emerald-500 to-teal-500 hover:from-green-600 hover:via-emerald-600 hover:to-teal-600 text-white shadow-lg hover:shadow-xl'
                      : 'bg-gradient-to-r from-gray-800 to-gray-900 hover:from-gray-700 hover:to-gray-800 text-white shadow-lg hover:shadow-xl'
                  }`}
                >
                  {loadingStates[plan.id] ? (
                    <div className="flex items-center gap-3">
                      <div className="animate-spin rounded-full h-5 w-5 border-2 border-white border-t-transparent"></div>
                      <span className="font-medium">Загрузка...</span>
                    </div>
                  ) : (
                    plan.cta
                  )}
                </Button>
              </div>
            ))}
          </div>
          )}
        </div>
      </section>

      {/* Enterprise Plan */}
      <section className="px-4 sm:px-6 lg:px-8 pb-16">
        <div className="max-w-4xl mx-auto">
          <div className="rounded-xl sm:rounded-2xl border-2 border-gray-300 bg-gradient-to-r from-gray-900 to-gray-800 p-6 sm:p-8 text-white">
            <div className="text-center mb-6 sm:mb-8">
              <h3 className="text-2xl sm:text-3xl font-bold mb-3 sm:mb-4">{enterprise.name}</h3>
              <p className="text-lg sm:text-xl text-gray-300">{enterprise.description}</p>
            </div>
            
            <div className="grid md:grid-cols-2 gap-6 sm:gap-8 mb-6 sm:mb-8">
              <div>
                <h4 className="text-base sm:text-lg font-semibold mb-3 sm:mb-4 text-blue-300">Что включено:</h4>
                <ul className="space-y-2 sm:space-y-3">
                  {enterprise.features.slice(0, 4).map((feature: string, idx: number) => (
                    <li key={idx} className="flex items-center gap-2 sm:gap-3">
                      <Check className="h-4 w-4 sm:h-5 sm:w-5 text-blue-400 flex-shrink-0" />
                      <span className="text-sm sm:text-base text-gray-200">{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>
              <div>
                <h4 className="text-base sm:text-lg font-semibold mb-3 sm:mb-4 text-blue-300">Дополнительно:</h4>
                <ul className="space-y-2 sm:space-y-3">
                  {enterprise.features.slice(4).map((feature: string, idx: number) => (
                    <li key={idx} className="flex items-center gap-2 sm:gap-3">
                      <Check className="h-4 w-4 sm:h-5 sm:w-5 text-blue-400 flex-shrink-0" />
                      <span className="text-sm sm:text-base text-gray-200">{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
            
            <div className="text-center">
              <Button asChild variant="outline" size="lg" className="border-white text-white hover:bg-white hover:text-gray-900 px-6 sm:px-8 py-2.5 sm:py-3 text-base sm:text-lg">
                <Link href="/app/dashboard">Обсудить условия</Link>
              </Button>
            </div>
          </div>
        </div>
      </section>

      {/* Features Comparison */}
      <section className="px-4 sm:px-6 lg:px-8 pb-16 bg-white">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-12">
            <h2 className="text-3xl font-bold text-gray-900 mb-4">
              Сравнение возможностей
            </h2>
            <p className="text-xl text-gray-600">
              Выберите функции, которые нужны именно вашему бизнесу
            </p>
          </div>
          
          <div className="overflow-x-auto">
            <table className="w-full">
              <thead>
                <tr className="border-b border-gray-200">
                  <th className="text-left py-4 px-6 text-lg font-semibold text-gray-900">Функция</th>
                  <th className="text-center py-4 px-6 text-sm font-medium text-gray-600">Старт</th>
                  <th className="text-center py-4 px-6 text-sm font-medium text-gray-600">Про</th>
                  <th className="text-center py-4 px-6 text-sm font-medium text-gray-600">Бизнес</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-gray-100">
                {[
                  { feature: 'Калькулятор на сайте', start: true, pro: true, business: true },
                  { feature: 'Форма заявок', start: true, pro: true, business: true },
                  { feature: 'Базовая CRM', start: true, pro: true, business: true },
                  { feature: 'SMS-уведомления', start: false, pro: true, business: true },
                  { feature: 'Неограниченные заявки', start: false, pro: true, business: true },
                  { feature: 'Расширенная аналитика', start: false, pro: true, business: true },
                  { feature: 'Webhook интеграции', start: false, pro: true, business: true },
                  { feature: 'Команда пользователей', start: false, pro: false, business: true },
                  { feature: 'API доступ', start: false, pro: false, business: true },
                  { feature: 'Персональный менеджер', start: false, pro: false, business: true }
                ].map((row: any, index: number) => (
                  <tr key={index} className="hover:bg-gray-50">
                    <td className="py-4 px-6 font-medium text-gray-900">{row.feature}</td>
                    <td className="py-4 px-6 text-center">
                      {row.start ? <Check className="h-5 w-5 text-green-500 mx-auto" /> : <span className="text-gray-300">—</span>}
                    </td>
                    <td className="py-4 px-6 text-center">
                      {row.pro ? <Check className="h-5 w-5 text-green-500 mx-auto" /> : <span className="text-gray-300">—</span>}
                    </td>
                    <td className="py-4 px-6 text-center">
                      {row.business ? <Check className="h-5 w-5 text-green-500 mx-auto" /> : <span className="text-gray-300">—</span>}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      {/* FAQ Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 bg-gradient-to-br from-green-50 to-emerald-50">
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-12">
            <h2 className="text-3xl font-bold text-gray-900 mb-4">
              Частые вопросы о тарифах
            </h2>
            <p className="text-xl text-gray-600">
              Ответы на популярные вопросы о ценообразовании и возможностях
            </p>
          </div>
          
          <div className="grid gap-6">
            {[
              {
                question: 'Можно ли изменить тариф в процессе использования?',
                answer: 'Да, вы можете изменить тариф в любое время через личный кабинет. При переходе на более дорогой план оплата производится пропорционально, при переходе на более дешевый — разница зачисляется в счет следующего периода.'
              },
              {
                question: 'Что происходит при отключении подписки?',
                answer: 'При отключении подписки вы сохраняете доступ к базовому тарифу "Старт" навсегда. Все ваши данные и настройки сохраняются. Вы можете восстановить подписку в любое время.'
              },
              {
                question: 'Есть ли ограничения по количеству заявок?',
                answer: 'В тарифе "Старт" есть ограничение по количеству заявок. В тарифах "Про" и "Бизнес" заявки не ограничены. Мы рекомендуем начать с бесплатного тарифа для оценки возможностей.'
              },
              {
                question: 'Поддерживаете ли вы индивидуальные решения?',
                answer: 'Да, для корпоративных клиентов мы разрабатываем индивидуальные решения под ваши потребности. Это может включать специальные интеграции, дополнительные функции или адаптацию под ваши бизнес-процессы.'
              },
              {
                question: 'Как происходит техническая поддержка?',
                answer: 'В тарифе "Старт" доступна email поддержка. В тарифах "Про" и "Бизнес" — приоритетная поддержка через чат и телефон. Корпоративным клиентам предоставляется персональный менеджер.'
              }
                            ].map((item: any, index: number) => (
              <details key={index} className="group rounded-xl border border-gray-200 bg-white">
                <summary className="cursor-pointer list-none p-6 hover:bg-gray-50 transition-colors">
                  <div className="flex items-center justify-between">
                    <h3 className="text-lg font-medium text-gray-900 pr-4">{item.question}</h3>
                    <div className="h-6 w-6 text-gray-400 group-open:rotate-180 transition-transform">
                      ▼
                    </div>
                  </div>
                </summary>
                <div className="px-6 pb-6">
                  <p className="text-gray-600 leading-relaxed">{item.answer}</p>
                </div>
              </details>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 bg-gradient-to-r from-green-600 to-emerald-600">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl md:text-4xl font-bold text-white mb-6">
            Готовы начать?
          </h2>
          <p className="text-xl text-blue-100 mb-8 leading-relaxed">
            Выберите подходящий тариф и начните автоматизировать обработку заявок уже сегодня
          </p>
          <div className="flex justify-center mb-8">
            <Button asChild size="lg" className="px-10 py-4 text-xl font-bold bg-white text-green-600 hover:bg-green-50 border-2 border-white hover:border-green-200 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105">
              <Link href="/app/dashboard">Войти в кабинет</Link>
            </Button>
          </div>
          <p className="text-sm text-green-200">
            Нужна консультация? Свяжитесь с нами: support@dezexpert.pro
          </p>
        </div>
      </section>
      </div>

      {/* Footer */}
      <Footer />
    </div>
  );
}


